# Server
server.port=${SERVER_PORT:8081}

# H2 in-memory database for local development
spring.datasource.url=${DB_URL:jdbc:h2:mem:authdb}
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.H2Dialect}
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.h2.console.enabled=true

# JWT
app.jwt.secret=${JWT_SECRET:357638792F423F4428472B4B6250655368566D597133743677397A2443264629}
app.jwt.access-token-expiration-ms=${JWT_ACCESS_EXPIRATION_MS:60000}
app.jwt.refresh-token-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:600000}
